# Devon — Project State Snapshot

> **What this file is.** A single-page current-state snapshot of Devon: what it is, what's done, what's in flight, where the canonical docs live, and the cultural/language defaults future sessions should respect. Updated on every `/doc_sync`. Pair with [`HISTORY.md`](./HISTORY.md), which is the chronological log of how we got here.

---

## What Devon is

**Devon** — on-premise corporate platform for digitizing internal document workflows in Uzbek organizations. Replaces paper-based "soglasovaniya" (collaborative approval) with a fully digital, auditable system. Codename **PLYMA** in earlier specs; **PLYMO** before that. The product is the same; the name evolved.

- **Audience:** Government bodies, state-owned enterprises, banks, holding companies, ministries — 50+ employees, hierarchical structure, existing paper workflow.
- **Deployment:** Fully on-premise (data sovereignty is a hard constraint).
- **Languages:** Uzbek-first. Russian and English are secondary / planned.
- **Roles:** Super Admin · Department Head · Employee.
- **Org hierarchy (canonical, 4 levels):** Departament → Boshqarma → Bo'lim → Sho'ba.

---

## Module status (v1.0 — ships with 8 modules)

| # | Module | Status |
|---|---|:---:|
| 1 | User & Authentication | ✅ in v1.0 spec |
| 2 | Document Management | ✅ in v1.0 spec |
| 3 | Electronic Digital Signature (ERI) | ✅ in v1.0 spec |
| 4 | Approval Workflow ("List soglasovaniya") | ✅ in v1.0 spec |
| 5 | Task Delegation (Kanban) | ✅ in v1.0 spec |
| 6 | Organizational Structure | ✅ in v1.0 spec |
| 7 | Integration & Export | ✅ in v1.0 spec |
| 8 | Incoming/Outgoing Letters | ✅ in v1.0 spec |

All eight are specified in [`docs/product-specification.md`](../docs/product-specification.md). "Spec'd" ≠ "shipped" — engineering implementation is separate from this doc set.

### Roadmap

| Release | Scope |
|---|---|
| **v1.0 (current spec)** | All 8 modules above, Uzbek UI only |
| **v1.1 (planned)** | Russian-language UI, mobile-responsive refinement, throttling/rate limiting |
| **v1.2 (planned)** | 2FA, advanced reporting dashboard, SSO (enterprise) |
| **Post-v1.2** | Native mobile apps, AI-assisted document classification, e-gov integrations |

---

## Canonical documents — where each truth lives

| Topic | File | Audience |
|---|---|---|
| Product overview, modules, roles, roadmap | [`README.md`](../README.md) | All stakeholders |
| **Product specification (canonical)** | [`docs/product-specification.md`](../docs/product-specification.md) | Product, BA, Eng, QA, Sales |
| **HR & ERI module — focused TZ (Uzbek)** | [`docs/Plyma TZ xodim kiritish.docx`](../docs/Plyma%20TZ%20xodim%20kiritish.docx) | Product, BA, Eng, QA — canonical spec for the dashboard's first milestone |
| **Updated TLH — Laravel stack + BPMN (Uzbek)** | [`docs/Plyma 19.03.2026.docx`](../docs/Plyma%2019.03.2026.docx) | Canonical spec for the dashboard's **second milestone** (document flows §2.2–2.4/§2.7, BPMN §3.1–3.4); also carries the Laravel-stack architecture, team plan and to-do/price blocks |
| **BPMN diagrams (extracted PNGs)** | [`docs/bpmn/`](../docs/bpmn/) | The 4 swim-lane charts from the TLH, named + indexed for direct reference |
| Business processes (swim-lane flows) | [`docs/business-processes.md`](../docs/business-processes.md) | BA, QA, customer implementation |
| Functional use cases (UC-01 … UC-20) | [`docs/use-cases.md`](../docs/use-cases.md) | QA, BA, Product |
| Glossary (Uzbek/Russian terms, pronunciation, naming history) | [`docs/glossary.md`](../docs/glossary.md) | Non-Uzbek-speaking team members |
| Competitive analysis & positioning | [`docs/competitive-analysis.md`](../docs/competitive-analysis.md) | Sales, Product, BA |
| Marketing landing page (Uzbek) | [`landing/index.html`](../landing/index.html) | Marketing, Sales, Web |
| **Dashboard build prompt set** | [`docs/dashboard-prompts/`](../docs/dashboard-prompts/) | AI assistants and contributors building the SPA — master prompt + 15 sequential step prompts |
| **Dashboard QA notes** | [`dashboard/QA_NOTES.md`](../dashboard/QA_NOTES.md) | QA findings, known demo-acceptable limitations, Lighthouse scores, pending observational checks |
| Workflow orchestration (how to work on Devon) | [`CLAUDE.md`](../CLAUDE.md) | AI assistants, contributors |
| Legacy technical spec (superseded by the 19.03.2026 TLH) | `docs/Plyma_Technical_Spec_v1.0.docx` | Historical reference |
| Session work log | [`ai_context/HISTORY.md`](./HISTORY.md) | Internal, contributors |
| Build lessons (decisions future sessions must respect) | [`ai_context/LESSONS.md`](./LESSONS.md) | AI assistants, contributors |

**Source-of-truth rule:** if a `docs/` file conflicts with anything else (this file included), the doc wins — fix the doc first, then update derivative artifacts.

---

## Competitive landscape (snapshot)

Closest direct competitor: **EDoc** (`edoc.uz`) — same market, same e-imzo (ERI) requirement. Devon's differentiation against them: integrated experience (documents + signatures + approvals + tasks in one product), modern UI, audit completeness.

Broader alternative: **Bitrix24** — but only when the buyer's primary need is sales-team management with documents as a side feature. Devon is the opposite: documents-and-approval as the primary, with tasks adjacent.

International tier (M-Files, DocuWare): great products, no Uzbek presence, no e-imzo. Devon wins on local fit.

Full breakdown in [`docs/competitive-analysis.md`](../docs/competitive-analysis.md).

---

## Brand voice & language defaults

- **Uzbek-first in all customer-facing copy.** Don't ship `[NEEDS_TRANSLATION]` placeholders in Uzbek. Russian and English follow as translation passes.
- **The slogan** — *Rivolanish intizom bilan!* ("Development through discipline") — appears in the footer in italic accent colour.
- **No tech-stack mentions on the landing page or product docs.** No Laravel, Livewire, PostgreSQL, etc. The audience is decision-makers, not developers. Engineering details live in code and ADRs, not in README/landing/product-spec.
- **Use the project glossary** ([`docs/glossary.md`](../docs/glossary.md)) for terminology. Prefer `Ichki server` or `Mahalliy yechim` over `On-premise` in Uzbek copy. Prefer `Hujjat aylanmasi` over `Document flow`. Etc.

---

## Landing page — current state

`landing/index.html` is a single self-contained HTML file deployed via GitHub Pages (`.github/workflows/deploy.yml` and `static.yml`). Current state:

- **Visual style** — wio.io-inspired with warm pastel section rotation (cream → white → peach → mint → navy → cream → lavender). Inter + Fraunces from Google Fonts. No external JS libraries.
- **Brand assets** — `landing/favicon.svg` (vector "D" initial in emerald + cinnamon diamond accent, echoes the navbar wordmark). `theme-color` meta set to emerald `#1F4E3F` for mobile browser chrome tinting.
- **Device mockups** — silver MacBook Pro frames (matching the user's reference photo) used across hero, document list, and Kanban sections. Realistic iPhone 15 Pro frame for the ERI signing flow. All animations smoothly looped with `calcMode="spline"` + eased `keySplines`.
- **Imkoniyatlar (Features) bento section** — 6 bento cards. Two charts are richly animated:
  - **Tashkiliy tuzilma** — 4-level org tree (Departament → Boshqarma → Bo'lim → Sho'ba) with a walking active path that highlights each node in sequence, ending in an amber pulse-ring on the destination Sho'ba.
  - **Vazifa doskasi (Kanban)** — 4 columns, cards with priority chips/due dates/avatars, a card travels Col 1 → 4 with column-arrival flashes, dashed drop-slot, and a cursor following the path.
- **Mobile responsive** — hamburger menu below 820px (full-screen overlay outside `<header>` to avoid `backdrop-filter` stacking trap). Breakpoints at 1100/820/768/480.
- **Hero chips** — floating decorative cards anchored with `calc(50% ± 510px)` so they never overlap the centered headline; hidden below 1400px.
- **Hero copy** — eyebrow reads "Mahalliy yechim · O'zbekiston uchun yaratilgan" (was "On-premise · ..." until corrected for Uzbek-first feel).

---

## Dashboard — demo build (scaffold landed)

A customer-facing product demo, deployed alongside the landing page on GitHub Pages, ships in the same site:

- `sardorallaberganov.github.io/devon-landing/` → landing
- `sardorallaberganov.github.io/devon-landing/dashboard/` → React SPA covering the 4 flows from the HR & ERI TZ

The repo is named `devon-landing` (legacy from when the landing was the only deliverable). The live URL prefix is therefore `/devon-landing/dashboard/`, not the `/Devon/dashboard/` the build prompts were originally written against. `vite.config.ts` `base`, `dashboard/index.html` favicon `href`, and `dashboard/public/404.html`'s `pathSegmentsToKeep` comment all reference the actual repo name. If the repo is ever renamed back to `Devon`, all three lines flip together — comment in `vite.config.ts` spells this out.

**Scope** — full demo of all four flows: Tarkibiy bo'linmalar CRUD · Xodim 4-step wizard · Assignment transfers + timeline · ERI certificate management (Kanban + mocked PFX upload).

**Stack (locked)** — Vite 8 + React 19 + TypeScript 6 + Tailwind CSS 4 (CSS-first config, no `tailwind.config.ts`) + shadcn/ui (v4 CLI: `--template vite --base radix --preset nova`) + react-router-dom v6 (BrowserRouter, `/dashboard/` sub-path, SPA 404 fallback) + Zustand + react-hook-form + zod + react-i18next + date-fns + lucide-react. (Originally locked to React 18 / TS 5 / Vite 5 / Tailwind 3; bumped to current ecosystem during steps 01–02.)

**Visual direction** — "brand-warm chrome, neutral work surfaces" — sidebar, top bar, page headers, hero stat cards inherit the cream + emerald palette from `landing/index.html`; data tables, form fields, wizard step content shift to white surfaces with tighter density. Devon's CSS tokens (`--cream`, `--emerald`, `--cinnamon`, `--signal`, etc.) map to shadcn's semantic vars (`--background`, `--primary`, `--accent`, …) so every primitive renders branded automatically.

**Mobile-first (non-negotiable)** — every screen tested at 360 / 390 / 768 / 1024 / 1280 / 1920px. Sidebar collapses to a `Sheet` drawer below `lg`. Data tables become card stacks below `md`. The employee creation wizard becomes a full-screen route on mobile with a sticky bottom CTA above the iOS safe area. The certificates Kanban collapses to `Tabs` (one column at a time) below `lg`.

**Auth model** — single HR_ADMIN demo user (`admin@devon.uz` / `Demo2026!`, credentials shown on the login screen). Other roles exist in the data model and seed data but aren't accessible via login in v1 of the demo.

**i18n** — react-i18next scaffolded from day one. UZ JSON populated; RU and EN files stubbed and rely on UZ fallback. v1.1 roadmap fills Russian.

**Mock backend** — there is no real server. All "API" calls hit a typed wrapper over `localStorage` with simulated 200–600ms latency and 3% random failure simulation. Seeded with ~6 root departments / 25 units / 30 employees with realistic Uzbek FIO / 25 certificates / 60+ audit entries. A "Reset demo" action in the user menu re-seeds cleanly.

**Status** — Build prompt set complete (17 files, ~7,700 lines in [`docs/dashboard-prompts/`](../docs/dashboard-prompts/)). **Steps 01–15 landed (2026-05-25 → 2026-06-01).** Dashboard MVP is live at `sardorallaberganov.github.io/devon-landing/dashboard/`; observational QA sweep (six viewports, Lighthouse, real device) is tracked in [`dashboard/QA_NOTES.md`](../dashboard/QA_NOTES.md) and pending the human operator.

**Foundation (steps 01–06):** scaffold · Tailwind v4 · shadcn/ui (31 primitives — `form` still missing from Nova preset, see open questions) · react-i18next (UZ canonical, RU/EN stubs) · react-router-dom v6 with `basename` derived from Vite's `import.meta.env.BASE_URL` (currently `/devon-landing/dashboard/` — was `/Devon/dashboard/` in early steps; see post-step-14 fixes for the rename) · Zustand auth store · `RequireAuth` deep-link preserver via `?from=` · mobile-first split-pane login · AppShell (Sheet drawer on mobile, persistent 240 px column on `lg+`, full-width `<main>` per [`LESSONS.md`](./LESSONS.md)) · mock-backend foundation (zod schemas + namespaced `devon.dashboard.*` localStorage tables + 200–600 ms simulated latency + 3 % `maybeFail()` on mutations + a **versioned seed flag** — `SEED_VERSION = '10'` (bumped to `'10'` for the `positionInstruction` backfill, see below), bumped on any identity-changing seed edit so existing browsers re-seed silently on next load).

**Seed contents (post step 20):** 26 units — 25 across the 4-level hierarchy + the root-level **Devonxona** (`DEV-01`, type `OTHER`) · 31 employees with hand-crafted Uzbek FIOs (the HR_ADMIN is **Pulatov Asilbek Karimovich**; the Devonxona persona is **Yusupova Nilufar Baxtiyorovna** with `roles: ['ROLE_DEVONXONA']` — login credentials stay `admin@devon.uz` / `Demo2026!`, hardcoded independent of FIO) · 31 primary assignments · 9 units carry `headEmployeeUuid` (wired from the `HEAD_POSITION_IDS` set in step 16; previously no unit had a head set) · every employee carries `employmentOrderExtract` **and `positionInstruction`** metadata (fake hiring-order file names like `buyruq_2024-17_kochirma.pdf` + job-instruction `lavozim_yoriqnomasi_<positionId>.pdf`; no bytes — both required at creation since the post-M2 HR-attachments change) · 26 certificates in the 19 ACTIVE / 4 PENDING_APPROVAL / 2 EXPIRED / 1 REVOKED split (the 19th ACTIVE belongs to the Devonxona persona) · ~70 audit entries spanning 30 days · 15 positions (`POS-CHANCELLERY` added) · ~20 notifications across the five POV personas (mix read/unread, all 13 `NotificationType`s; every notification resolves to a real seeded row since step 20 — though `/letters/:uuid` itself ships in step 21, so letter deep-links still fall through to home) · **5 document templates** (BUYRUQ / XIZMAT_XATI / MALUMOTNOMA / ARIZA / BILDIRISHNOMA, fixed uuids `…c001`–`…c005`, Uzbek bodies with `{{PLACEHOLDER}}` tokens) · **12 documents** walking BPMN 3.4 (2 DRAFT / 3 IN_REVIEW / 1 REJECTED / 2 APPROVED / 2 SIGNED / 2 CLOSED — every persona's `/approvals` queue non-empty; the six notification-referenced docs sit on fixed uuids `…d001`–`…d006`; TEMPLATE docs persist their raw `values` since step 19 so the edit/rework loop prefills) with internally-consistent approval steps · **3 signature records** (2 backing the SIGNED docs + the Rahbar's ERI on the dispatched outgoing letter `…f003` = CH-2026/0001) · **12 letters** walking BPMN 3.3 (9 incoming covering every lifecycle status — REGISTERED / ROUTED / ASSIGNED-overdue / IN_PROGRESS / EXECUTED / ON_SIGNATURE / RESPONDED / CLOSED / CLOSED_NO_RESPONSE — + 3 outgoing DISPATCHED; the prompt's 10-letter table grew by 2 because `…f001` = K-2026/0002 must sit post-acceptance per its step-16 notification story (→ RESPONDED) and CLOSED_NO_RESPONSE needed a seed row for status completeness; `…f002` = K-2026/0004 ON_SIGNATURE; all routed letters walk the Backend Bo'lim branch so the five personas own every action). The five personas live behind fixed literal employee UUIDs exported as `PERSONAS` from `seed.ts` (HR_ADMIN · RAHBAR = Karimov Bekzod, IT Departament head · BOLIM_BOSHLIGI = Akhmedov Akmal, Backend Bo'lim head · DEVONXONA = Yusupova · XODIM = Sobirova Dilnoza, API Sho'ba dasturchisi — all in one branch so an approval chain walks a single subtree). Auth store carries a `refreshSessionUser()` method called from [`main.tsx`](../dashboard/src/main.tsx) right after `seedIfEmpty()` — silently reconciles any persisted session's cached `fullName` against the freshly-seeded employee record so renames flow through without forcing logout.

**Dashboard home at `/` (step 07):** `PageHeader` greeting (`Salom, {firstName}!` parsed from the seeded `Surname Given Patronymic`) · null-rendered `ExpiringCertsAlert` (30-day horizon over ACTIVE certs) · 4-card `StatsRow` (Faol xodimlar · Tarkibiy bo'linmalar · Faol ERI · Tasdiqlash kutilmoqda — tone rotation emerald → default → signal → cinnamon) · 4-up `QuickActions` grid · full-width `RecentActivityCard` sourced from `listAudit({ limit: 8 })` with lucide-iconed action tiles + localised verb forms (13 audit-action translations) + `formatRelative()` timestamps. Four reusable common state components shipped alongside: `LoadingState` / `EmptyState` / `ErrorState` / `StatCard`.

**Flow 1 at `/units` (step 08):** recursive desktop tree with **deep-descendant search highlight** (precomputed ancestor set from each hit's `path`, so a match five levels down auto-expands the entire chain) · 2-level mobile accordion · create/edit form via `Dialog`-on-desktop / bottom-`Sheet`-on-mobile (`ResponsiveDialog` helper) with `react-hook-form` + `zodResolver` · right-side `UnitDetailsSheet` (head-name resolved from `headEmployeeUuid` lookup, child counts live, action buttons stacked vertically to avoid cramping inside the 448 px drawer) · debounced search by name + code + shortName · status filter (ACTIVE default / ARCHIVED / ALL) · parent dropdown client-side-excludes self + descendants · type dropdown auto-snaps when parent change makes the current selection invalid · archive guard against non-terminated employees. Backed by hardened mock-backend mutations: `createUnit` / `updateUnit` enforce `MAX_UNIT_DEPTH = 7`, name-unique-within-parent (case-insensitive), cycle prevention via `path` traversal, full descendant path + level recompute when parent moves, all surfaced via a typed `UnitValidationError` with codes `cycle | duplicate-name | max-depth | invalid-parent` that the UI maps to `dashboard:units.errors.*` toasts. Three new reusable common components shipped here: `ResponsiveDialog` (Dialog ≥md / bottom-Sheet <md, with `gap-0 p-0` on the panel + explicit `p-6` header / `px-6 py-5` body / `px-6 pt-4 pb-safe` footer bands separated by borders — same pattern as the right-side `UnitDetailsSheet`, so future form sheets like the step-10 employee wizard inherit the padding for free) · `StatusBadge` (11 status kinds: ACTIVE / ARCHIVED / DRAFT / PENDING_APPROVAL / APPROVED / REJECTED / EXPIRED / REVOKED / SUSPENDED / TERMINATED / ON_LEAVE) · `SearchInput` (300 ms debounce).

**Cross-cutting polish landed alongside step 08:** shadcn `Input` / `SelectTrigger` / `Button` primitives bumped to `h-10` default (was `h-8`) for mobile-first 40 px touch targets and to fix a CSS-specificity trap where `SelectTrigger`'s `data-[size=default]:h-8` silently beat consumer `className="h-11"` overrides — every form control across the dashboard now aligns at 40 px without consumers needing to override; see [`LESSONS.md`](./LESSONS.md) "Form-control height" for the trap and [`LESSONS.md`](./LESSONS.md) "Bump `SEED_VERSION`" for the seed-versioning rule that backstops fixture renames.

**Flow 2 part A at `/employees` (step 09):** searchable + filterable + paginated employee list. shadcn `Table` on `md+` (FIO + email + avatar · Bo'linma · Lavozim · masked-grouped JSHShIR · status badge with hover-row highlight + click-to-`/employees/:uuid`) · card stack on `<md` with 64 px+ tap targets. Filter panel adapts: inline row on desktop, bottom-`Sheet` with draft state + Apply / Reset on mobile (using the band-padding pattern from yesterday's `ResponsiveDialog` fix — no `-mx-6 px-6` bleed-edge tricks). Position IDs resolve to localised names via `listPositions()` lookup. New reusable `Pagination` component (icon-sm prev/next + range text) used here and reusable from step 11 onwards. Backed by the existing `listEmployees({ search, unitUuid, status })` filters; `employmentType` filter + page slice handled client-side (cheap for the 30-employee demo).

**Flow 2 part B at `/employees/new` (step 10):** full-screen mobile / centred-card desktop wizard covering the 4-step employee creation flow from TZ §4.4. Steps: Shaxsiy (names + gender + birthDate + PINFL with live ✓/✗ dedup) → Aloqa (phones with auto-mask + corporate email with live dedup against `findUserByEmail`) → Ish o'rni (unit Combobox + type-filtered position Select + employment-type RadioGroup + hireDate + role + required **Buyruqdan ko'chirma** attachment — metadata-only, PDF/JPG/PNG ≤ 10 MB, added 2026-06-11) → Kirish (auto-derived login + `crypto.getRandomValues`-based 12-char password generator + Fisher-Yates shuffle + strength meter + copy/show-hide + notify-SMS/email checkboxes) → Ko'rib chiqish (4 summary cards, each with Edit→`setCurrent` jump). Submit calls `createEmployeeFull` which now enforces PINFL + corporate-email uniqueness (typed `EmployeeValidationError` with codes `pinfl-taken | email-taken`) and creates `Employee + User + Assignment + Audit` atomically. Each step is a `<form id="wizard-step-N">` + `<Button form="..." type="submit">` pattern (idiomatic react-hook-form, cleaner than imperative onValid callbacks). Route uses a new `ProtectedNoShell` wrapper that skips AppShell — the wizard renders its own chrome (top bar + stepper + sticky `pb-safe` footer).

**Shadcn `form` primitive finally landed in step 10** (`src/components/ui/form.tsx`), filling the gap from step 02's Nova-preset skip. Wizard fields use raw `register`+`setValue`+`watch` for compactness; the `FormField`/`FormItem`/`FormLabel`/`FormControl`/`FormMessage` wrappers are available for forms that benefit from them (nested arrays, dynamic field lists). New common `Combobox` component (Command-in-Popover desktop / Command-in-Sheet mobile) shipped here, reusable from step 11 onwards. `.no-scrollbar` utility added to `index.css` for the wizard stepper's horizontal pill scroll on mobile.

**Flow 3 at `/employees/:uuid` + `/employees/:uuid/transfer` (step 11):** 4-tab employee profile (Ma'lumotlar · Bo'linmalar · ERI kalitlari · Tarix) with an identity hero band (avatar with bg-emerald initials · FIO + email + phone · StatusBadge + grouped PINFL · Transfer CTA disabled when TERMINATED). Info tab is a mobile-stacked / desktop 2-col description list of 11 fields backed by a `ResponsiveDialog`-wrapped `UpdateEmployeeSheet` (re-uses wizard step-1+2 fields, drops PINFL since it's locked post-creation, email dedup runs only on change) + a red-outline Terminate button that opens an `AlertDialog` confirming the cert-revocation cascade (TZ §6.6). Bo'linmalar tab is a vertical `AssignmentTimeline` (border-l-2 rail · `-left-[31px]` dot · `bg-emerald ring-4 ring-emerald-soft` for active rows / `bg-muted-foreground` for closed · Primary + assignment-type badges · unit-link chevron · Progress bar when workload < 100 · italic muted reason). ERI tab is a lightweight list of certificates with StatusBadge + valid window + serial number; Tarix tab is `listAudit({ resourceUuid })` rendered with the same `ACTION_ICON` map as `RecentActivityCard`. Transfer page lives at `/employees/:uuid/transfer` under `ProtectedNoShell` (same chrome shape as the wizard — mobile X+title topbar, desktop back-link+title+subtitle band, sticky `pb-safe` footer); form fields per TZ §5.4 (new unit Combobox · type-filtered position Combobox · startDate · paired Slider+numeric workload · 4-card RadioGroup for assignment type · auto-toggled close-old Checkbox · optional Textarea reason). Backed by hardened mock-backend `transferEmployee`: new exported `MAX_TOTAL_WORKLOAD_PERCENT = 150` cap with typed `AssignmentValidationError('workload-exceeded')` thrown when projected total breaks the cap; auto-demotion of existing open `isPrimary` row when new transfer is PRIMARY but `closeOldAssignment=false` (prevents two-open-primaries state); rich audit `changes` block (`{ unit: { from, to }, position: { from, to } }`) for step 13's audit diff view + flat `context` (`assignmentType` / `workloadPercent` / `closedOld` / `reason`). `listAudit` also gained a `resourceUuid` filter so the History tab scopes server-side instead of pulling all rows.

**Shadcn `slider` primitive landed in step 11** ([`src/components/ui/slider.tsx`](../dashboard/src/components/ui/slider.tsx)) — canonical Radix-backed Slider via the existing `radix-ui` umbrella package (no new dep). Used by the transfer workload field; reusable from step 12 onwards. The wizard's `<Button form={STEP_FORM_IDS[current]}>` external-submit pattern carries over to the transfer page (`<Button form={FORM_ID}>`).

**Flow 4 at `/certificates` + `/certificates/upload` (step 12):** 4-column ERI Kanban on desktop (≥ lg: PENDING_APPROVAL · ACTIVE · EXPIRED · REVOKED, each with a tinted header band + count Badge + scrollable card list; gracefully collapses to 2 cols at md/tablet widths) / underline-tabs single-column on mobile (same underline pattern as step 11's profile, count pills inline in each tab label). `CertificateCard` shows avatar with employee initials + issuer + validity window + StatusBadge + cinnamon `expiring-soon` pill (ACTIVE certs with < 30 days remaining); PENDING cards carry a Checkbox for bulk selection. Bulk-approve `Alert` band slides in when selection non-empty; loops `approveCertificate`, tolerates the 3% mock-network failure rate, toast reports actual success count. Card click opens a right-side `CertificateDetailsSheet` (UnitDetailsSheet parity from step 08) with metadata description-list + status-aware vertically-stacked action buttons: PENDING → Tasdiqlash + Rad etish; ACTIVE → Bekor qilish; EXPIRED / REVOKED / REJECTED → metadata-only (no footer). Three dialogs back the actions: `ApproveDialog` (AlertDialog with name-interpolated body), `RejectDialog` (ResponsiveDialog with Textarea + 5-char minimum reason validation), `RevokeDialog` (ResponsiveDialog with Select over `[EXPIRED, COMPROMISED, REPLACED, MANUAL]` — `EMPLOYEE_TERMINATED` excluded since it's set automatically by `terminateEmployee` cascade).

Upload page at `/certificates/upload` (under `ProtectedNoShell` — wizard/transfer parity) is a progressive-disclosure 4-step form (`<ol>` of numbered `<Step>` sections) backed by [`FakePfxParser.ts`](../dashboard/src/features/certificates/FakePfxParser.ts) (~50 lines, typed `FakePfxParseError`, exported `MAX_PFX_SIZE_BYTES = 100 * 1024`, 800–1500 ms simulated parse latency via `setTimeout`, `crypto.getRandomValues`-based hex for serial/thumbprint, hard-coded `YANGI TEXNOLOGIYALAR ILMIY-AXBOROT MARKAZI AJ` issuer, returns plausible X.509 metadata with the employee's PINFL/FIO mirrored so the round-trip passes the backend's pinfl-mismatch guard). Steps: Employee Combobox (pre-filled from `?employee=<uuid>` query — set by step-11's profile cert tab) → File picker (`.pfx, .p12`, ≤ 100 KB client-side gate) → Password input (`type=password`, autocomplete=off, "Parol serverga uzatilmaydi" hint) → "Faylni o'qish" button → Confirmation card with 6-row metadata description-list + cert-type Select (SIGNING / ENCRYPTION / BOTH) + `autoApprove` Checkbox (defaults ON for HR_ADMIN). Submit: 1.5 s mocked E-IMZO challenge-response (`ShieldCheck` pulse per master §17 "fake the WebSocket with a delay") → `uploadCertificate` → toast → navigate back to `/employees/:uuid` if we came from the profile, else `/certificates`. Backend hardened: `uploadCertificate` now enforces serial uniqueness (case-insensitive) + PINFL-mismatch guard via typed `CertificateValidationError` (codes `serial-taken | pinfl-mismatch`); `rejectCertificate` swapped from the lying `CERTIFICATE_APPROVED + context.decision='REJECTED'` shape to the new dedicated `CERTIFICATE_REJECTED` audit action; `listAudit`'s `resourceUuid` filter from step 11 already supported the step 11 profile history tab and now supports the upcoming step 13 audit-log view.

**`?upload=1&employee=<uuid>` auto-bounce** — step 11's profile cert tab CTA routes to `/certificates?upload=1&employee=<uuid>`; `CertificatesPage`'s `useEffect` detects the query param on mount and immediately `navigate(/certificates/upload?employee=<uuid>, { replace: true })` so the user lands on the upload form in one hop instead of getting dropped on the board first.

**Audit-action expansion** — `AuditAction` union gained `'CERTIFICATE_REJECTED'`; `RecentActivityCard` and `ProfileHistoryTab` icon maps both gained `ShieldOff` for it (visually distinct from `ShieldCheck` for APPROVED and `ShieldX` for REVOKED). Setup pays off in step 13's audit-log view filter chips.

**Build state:** `npm run build` → 2950 modules, 120.50 KB CSS, 1,129.67 KB JS / 313.41 KB gzip (post the post-M2 HR-attachments change: module count flat — the shared `MetaFileField` replaced `OrderExtractField` 1:1 — +~0.5 KB gzip; still comfortably under the 500 KB gzip target). Earlier post-step-12 Kanban DnD work still in place: `@dnd-kit/core@^6.3.1`, `@dnd-kit/sortable@^9.0.0`, and `@dnd-kit/utilities@^3.2.2` (`vite.config.ts` `optimizeDeps.include` forces them to pre-bundle together so they share one React instance — without it, adding a new `@dnd-kit/*` package while the dev server is running can land it in a separate optimized bundle with a null React and crash `SortableContext`). Sortable pinned at v9 because v10 has a Vite-incompatible bundle. PENDING_APPROVAL and ACTIVE columns support both cross-column drag (approve / revoke) AND within-column reorder via `@dnd-kit/sortable`'s `verticalListSortingStrategy`; terminal columns (EXPIRED / REVOKED) stay static with `cursor-not-allowed`. The optimistic UI patch + mock-backend `approveCertificate` / `revokeCertificate` both `unshift` the moved cert to the front of the certs table so the drop position is consistent before AND after `reload()`. A new `reorderCertificates(orderedUuids)` mutation persists within-column reorders (cosmetic, no audit entry). `select-none` on the `SortableCard` wrapper suppresses the browser's native blue text-selection highlight during drag. Mobile (<lg) keeps the underline-tabs flow — touch DnD is fragile and the single-column view has no other columns to drop into anyway.

**Build hygiene note:** The stray `package.json` + `package-lock.json` at the project root (documented in the step-12 build-hygiene note from the DnD work) were removed in step 14's pre-commit cleanup. `node_modules/` at the project root may still exist locally but is gitignored (the existing top-level `node_modules/` rule catches it); it can be removed manually with `rm -rf /Users/sardorallaberganov/Desktop/Projects/Devon/node_modules` for full tidiness.

**Step 13 at `/profile` + `/audit` (2026-05-27):** HR_ADMIN self-service profile + read-only audit log surface, both landing on routes that previously rendered the `Placeholder` (now removed from `router.tsx`).

- **`/profile`** — three-tab page (Asosiy ma'lumotlar · Parolni o'zgartirish · Tahrirlash so'rovlari). Identity hero band reuses the EmployeeProfilePage shape (cream-deep panel, emerald avatar with initials, FIO + email/phone + StatusBadge + position/unit metadata). Info tab is a 6-row description list backed by a `ProfileEditRequestForm` (ResponsiveDialog wrapping a `react-hook-form` with the same phone/email validation as the wizard). The form is role-branched: HR_ADMIN edits call `updateEmployee` directly with a "yangilandi" toast; everyone else calls `submitProfileChangeRequest` and the request appears in the third tab as PENDING. Password tab is `PasswordChangeForm` — `react-hook-form` + zod with 8-char / upper / lower / digit / special regex chain + cross-field "match confirm" + cross-field "differs from current" refinements, plus the wizard's `passwordStrength` scorer driving a Progress meter (destructive → cinnamon → emerald color rotation by score). Submit calls the new `changePassword(userUuid, current, next)` mutation; wrong-current surfaces as an inline form error on the `current` field (typed `PasswordValidationError('current-wrong')`), `MockNetworkError` becomes a toast, success resets the form. "Tahrirlash so'rovlari" tab carries a pending-count `Badge` on the trigger and renders the empty state for HR_ADMIN (their edits never queue) with a one-line explanation that the workflow exists for `ROLE_EMPLOYEE`.
- **`/audit`** — full-width PageHeader + a 4-column filter grid (resourceType `Select` · actor `Combobox` derived from distinct actors in the audit table · two native `<input type="date">`s for from/to). Filter reset CTA appears in the header only when any filter is active. List is a card stack on `<lg` and a 5-column `Table` (Vaqt · Aktor · Harakat · Resurs · Tafsilot) on `lg+`. Both layouts share `AuditEntryRow` with a `variant: 'card' | 'row'` discriminator. Each row's inline `DiffBlock` renders `changes` keyed by field name (`unit` / `position`) and resolves `unit` UUIDs to `nameUz` via a `Map<string, Unit>` passed in from the page. Newest-first sort + 50/page pagination (existing `Pagination` primitive). The mock-backend `listAudit` filter shape gained `dateFrom` / `dateTo` (ISO date strings; `dateTo` is widened to end-of-day so a same-day pick still returns rows). Date inputs cross-clamp via `min` / `max` so the picker can't yield an inverted range.
- **Mock-backend additions:** `submitProfileChangeRequest({ employeeUuid, fields }, actorUuid)` (writes a `PENDING` row + `PROFILE_CHANGE_REQUESTED` audit entry), `approveProfileRequest(uuid, actorUuid, decision, rejectionReason?)` (APPROVED branch applies the patch via `updateEmployee` then writes a `PROFILE_CHANGE_APPROVED` audit entry; REJECTED only writes audit), `changePassword(userUuid, current, next)` (SHA-256 compare via existing `sha256Hex`, throws typed `PasswordValidationError('current-wrong')` on mismatch, otherwise writes new hash + stamps `passwordChangedAt` + clears `mustChangePassword` + writes a `PASSWORD_CHANGED` audit entry). All three respect the `maybeFail()` 3% network-flake convention. The `Placeholder` component and the now-unused `useTranslation` + `PageHeader` imports were stripped from `router.tsx`.
- **No `SEED_VERSION` bump** — no fixture identity changes. Existing browsers keep their seed; the new pages render against the existing 30 employees / 1 HR_ADMIN user / ~70 audit entries.

**Step 14 GitHub Pages deploy (2026-05-27 → 2026-06-01, live now):** the existing `.github/workflows/deploy.yml` (which uploaded only `./landing`) was rewritten into a two-job build/deploy workflow that runs `npm ci && npm run build` inside `./dashboard`, stages a combined `pages-dist/` (landing files at the root, dashboard build at `pages-dist/dashboard/`), and uploads via `actions/upload-pages-artifact@v3` → `actions/deploy-pages@v4`. Node 20 LTS, `cache: 'npm'` against `dashboard/package-lock.json`. SPA deep-linking handled via the canonical [spa-github-pages](https://github.com/rafgraph/spa-github-pages) snippet — [`dashboard/public/404.html`](../dashboard/public/404.html) with `pathSegmentsToKeep = 2` (matches the `/devon-landing/dashboard/` URL prefix) rewrites any 404 into a `?/<path>` query that the handoff `<script>` inside [`dashboard/index.html`](../dashboard/index.html) (placed before the main module bundle so the first render sees the cleaned URL) decodes back into a real path via `window.history.replaceState`. [`.gitignore`](../.gitignore) gained explicit `dashboard/{node_modules,dist,.vite,.eslintcache}/` entries — top-level `node_modules/` and `dist/` already caught these, but spelling them out makes intent obvious and survives a future repo split. The redundant `.github/workflows/static.yml` workflow (which uploaded the entire repo root as the Pages artifact) was deleted to prevent the `concurrency: pages` race that would otherwise have it overwrite the dashboard build on every push. The stray `package.json` + `package-lock.json` at the project root (documented in the step-12 build-hygiene note and the `LESSONS.md` cleanup item) were removed; `node_modules/` at the root is gitignored so it can't accidentally land in a commit. Local smoke test via `npx serve` against a staged `/tmp/devon-pages/devon-landing/` tree confirmed `/devon-landing/`, `/devon-landing/dashboard/`, `/devon-landing/dashboard/favicon.svg`, and `/devon-landing/dashboard/assets/*.js` all return 200 with the right content; deep routes return 404 from `serve` as expected (GH Pages reroutes to `404.html` which carries the correct rewrite snippet).

**Post-step-14 fixes (2026-06-01):** first production deploy 404'd on every asset because `vite.config.ts` `base` was set to `/Devon/dashboard/` while the actual repo (and therefore Pages prefix) is `/devon-landing/dashboard/`. `5aae692` aligned `base`, dev-server `open`, favicon `href`, and the `404.html` comment in lockstep — `pathSegmentsToKeep = 2` stayed correct since the new prefix is still two segments. Landing CTAs were also normalised in two follow-on commits: `863d26b` pointed both `Kirish` links (desktop nav `.signin` line 426 + mobile menu `.mm-secondary` line 444) at `dashboard/login`, then `7452b03` re-pointed all three entry CTAs (`Kirish` desktop, `Kirish` mobile, `Demoga kirish` hero) at a single shared `dashboard/` href so they behave identically — `dashboard/` is the right target because `RequireAuth` on the home route bounces unauthenticated visitors to `/login` (the original target of the prior `Kirish` link), while authenticated returning visitors skip straight to home; pointing at `dashboard/login` directly would have re-shown the login screen even to already-authenticated users since `LoginPage` carries no already-authenticated guard. The other three landing CTAs (nav `Demo so'rash`, mobile menu `Demo so'rash`, architecture section `Modullar bilan tanishish`) intentionally keep pointing at `#demo` so the sales-lead email-capture funnel stays distinct from the live-demo path.

**Step 15 — final QA (2026-06-01, automated portion done):** the script-driven portion of the QA pass ran in this session: i18n grep audits (no Cyrillic leaks, no `toast.<level>("literal")` calls, no raw JSX text outside intentional `DEVON` brand wordmarks + `sr-only` accessibility labels), static content scans (no `PLYMA`/`PLYMO` legacy names, no tech-stack leaks, no `Date.toString` bypasses), bundle size (266.28 KB gzipped — comfortably inside the `<500 KB gzipped` target), and a dev-server console-error sweep across all 9 routes (all 200, zero warnings after the React Router future-flag fix). Three findings were fixed inline: (a) `<BrowserRouter future={{ v7_startTransition, v7_relativeSplatPath }}>` in [`App.tsx`](../dashboard/src/App.tsx) silenced the per-boot RR v7 future-flag warnings, (b) the English `Close` sr-only labels on every Sheet + Dialog corner-X close button were swapped for `Yopish` directly in the primitives per the `LESSONS.md` "edit shadcn primitives only when the default is wrong for every call site" rule, (c) the underline-tab triggers used by ProfilePage + EmployeeProfilePage had `data-active:font-semibold` causing the active tab to grow visibly wider on click (bold characters are wider than regular) and shift every sibling tab — fixed by extracting a new [`TabLabel`](../dashboard/src/components/common/TabLabel.tsx) common component that renders each label twice in the same CSS grid cell: a permanently `font-semibold invisible` copy that reserves the bold width, and a live copy that paints on top at the current state's weight. Layout no longer shifts when activating a tab. The observational portion — six-viewport sweep at 360 / 390 / 768 / 1024 / 1280 / 1920 px, Lighthouse mobile + desktop on every route, throttled-network skeleton check, forced-failure error-state sweep, offline check, keyboard-only wizard completion, `prefers-reduced-motion` check, focus-ring + contrast spot-checks, real-phone safe-area + hardware-back check, deep-link hard-refresh on the live deploy, "Reset demo" against the published bundle — is tracked as a checklist in [`dashboard/QA_NOTES.md`](../dashboard/QA_NOTES.md) "Pending observational sweep" section, with Lighthouse score placeholders for the human operator to fill.

**Milestone 2 — Electronic Document Management (in progress; steps 16–19 landed 2026-06-12, steps 20–21 landed 2026-06-13):** covers the TLH's document flows — **Flow 5** (BPMN 3.4: template/upload document creation, sequential kelishuv varaqasi, ERI signing, simulated daily archive — **fully demonstrable end-to-end since step 19**) and **Flow 6** (BPMN 3.3: Devonxona incoming/outgoing letters with routing → assignment → execution → acceptance → optional Rahbar ERI → dispatch with auto-numbering `K-2026/NNNN` / `CH-2026/NNNN` — **fully demonstrable end-to-end since step 21**: the registry landed in step 20, the `/letters/:uuid` detail page + BP-3 timeline + per-role action bar in step 21). Plan lives in [`docs/dashboard-prompts/`](../docs/dashboard-prompts/) steps **16–22** (+ M2 addenda in `00-master.md` §2/§9/§10/§11/§12/§15/§17 and the README decisions table). State machines follow `docs/business-processes.md` BP-3/BP-4 canon (`DocumentStatus: DRAFT→IN_REVIEW→REJECTED/APPROVED→SIGNED|CLOSED`, archive = `archivedAt` stamp; `LetterStatus` per the extended BP-3 list). Task delegation (BPMN 3.2) is milestone 3. Step **22 remains** (home integration + final M2 QA) — execute one step per session per the README rhythm.

**Step 16 — M2 foundations: POV switcher + notification centre (2026-06-12):** the two cross-cutting rails every M2 flow depends on; no document/letter domain logic yet (steps 17/20 own those tables).

- **POV switcher** — `useAuthStore` gained `actingAsEmployeeUuid` (persisted in the session blob so refresh keeps the POV) + `switchPov`/`resetPov` actions that write a `POV_SWITCHED` audit entry (actor = the real session user, `context.to` = persona key or `'self'`, resource = the session user row). New [`src/lib/acting.ts`](../dashboard/src/lib/acting.ts) exposes `useActingEmployee()` → `{ employee, user, roles, headedUnitUuids, isSelf }` — **every M2 page must resolve queues/action-visibility through it and pass `employee.uuid` as `actorUuid` into mutations**. UserMenu gained a "Rol almashtirish" persona picker (DropdownMenuSub ≥ md; inline radio section below — the user menu is a dropdown on all viewports, the step prompt's "mobile sheet" premise didn't match the code) listing the five personas as `<label> — <FIO>` with the current one checked. TopBar renders a cinnamon-soft chip (`Siz: {persona} sifatida`; persona-label-only below `sm`) with an inline × → `resetPov`. "Reset demo" clears the POV silently via `setState` (no audit noise after the table wipe); `refreshSessionUser()` drops a POV whose employee vanished in a reseed.
- **Notification centre** — `notifications` localStorage table + `appendNotification` (exported for steps 17–21, **no `maybeFail()`** — a notification must never be the flaky part of its mutation) / `listNotifications(recipient, {unreadOnly})` / `markNotificationRead` / `markAllNotificationsRead`. `NotificationsBell` replaces the topbar placeholder: unread badge (caps at `9+`, cinnamon), `Popover` ≥ md / bottom `Sheet` below, rows = type icon (13-entry lucide map) + `t(titleKey, params)` + `formatRelative` + emerald unread dot, header "mark all" action, scoped to the **acting persona** so switching POV switches the bell. No polling — refetch on open + on POV change (noted in code). Row click marks read (optimistic, flake-tolerant) and deep-links to `/documents/:uuid` / `/letters/:uuid` — those detail routes don't exist until steps 18–21, so the `*` catch-all currently redirects them to home (acceptable per the step prompt; seeded notifications use fixed placeholder resource UUIDs steps 17/20 will seed real rows under).
- **Sidebar + routes** — new "HUJJAT AYLANMASI" nav section (Hujjatlar `/documents` · Kelishuvlar `/approvals` · Xatlar `/letters`) with `ComingSoonPage` placeholders (PageHeader + EmptyState "Tez kunda") registered in `router.tsx` so nothing 404s; steps 18–20 replace them. The Kelishuvlar count badge arrives in step 19.
- **Types/i18n** — `Role` += `ROLE_DEVONXONA`, `AuditAction` += `POV_SWITCHED` (Drama icon added to all three audit icon maps; verb "rol almashtirdi"), new `AppNotification`/`NotificationType`. `uz.json` gained `dashboard.pov.*`, `dashboard.notifications.*` (header key is `heading` — `title` is the 13-entry per-type map, written in full now for steps 17–21) and three sidebar keys; dead `topbar.notifications`/`topbar.no-notifications` keys removed. Drive-by sync: the zod `auditActionSchema` had been missing `CERTIFICATE_REJECTED` since step 12 — added alongside `POV_SWITCHED`; `roleSchema` += `ROLE_DEVONXONA`; new `appNotificationSchema`.

**Step 17 — M2 Flow 5 backend: document domain in the mock backend (2026-06-12):** no screens — types, schemas, policy errors, mutations, audit + notification wiring, and seed, so steps 18–19 are pure UI work.

- **Types/schemas** — master-§15 document block in `domain.ts` (`DocumentSource` · `DocumentStatus` with the "editing APPROVED is out of scope, DRAFT/REJECTED only" comment · `Confidentiality` · `FileMeta` (shared with step-20 letters) · `TemplateField` · `DocumentTemplate` · `DocumentViewRecord` · `DocumentEntity` · `ApprovalDecision` · `ApprovalStep` · `SignatureRecord` with `resourceType: 'document' | 'letter'`); `AuditAction` += 8 `DOCUMENT_*` actions (icons added to all three audit icon maps; verbs in `uz.json`); `AuditResourceType` += `document`/`letter` (audit-page filter gained `document` only). Zod mirrors for everything.
- **Policy layer** — `DocumentValidationError` with 11 codes (`wrong-status`/`not-creator`/`not-participant`/`out-of-order`/`already-decided`/`comment-required`/`not-signer`/`not-recipient`/`cert-invalid`/`not-editable`/`not-deletable`), enforced inside every mutation against the **acting employee uuid** (M2 actors are employee uuids — audit actor names resolve via the new `employeeNameFor`, not `actorNameFor`). Signed-document protection: `deleteDocument` only removes the creator's own DRAFT; no code path deletes a non-DRAFT row. Localized toasts ready at `dashboard.documents.errors.*`.
- **API** — reads `listDocumentTemplates` / `listDocuments` / `getDocument` (composed `DocumentDetail`) / `listMyApprovals` (discriminated `decision`/`signature`/`acceptance` queue rows) / `recordDocumentView` (view-once, no `maybeFail`); mutations `createDocument` (auto-number `HJ-2026/NNNN`) / `updateDraftDocument` (rebuilds the *upcoming* round's PENDING steps; decided rounds immutable) / `submitDocumentForReview` (implicit-APPROVED branch when `!requiresApproval`; resubmit bumps `round` and clones the halted chain) / `decideApproval` (strictly sequential; REJECTED needs a comment and freezes the round) / `signDocument` (ACTIVE-own-cert check, `SignatureRecord`, SIGNED + immediate `archivedAt`) / `acceptDocument` (no-ERI → CLOSED + `archivedAt`) / `emailDocument` / `deleteDocument`. Acceptance-flavoured sign requests reuse `DOC_SIGN_REQUESTED` with the new `dashboard:notifications.title.DOC_ACCEPT_REQUESTED` titleKey.
- **`renderTemplate.ts`** ([`src/features/documents/renderTemplate.ts`](../dashboard/src/features/documents/renderTemplate.ts)) — pure sync `{{PLACEHOLDER}}` substitution, unknown keys → `«—»`; shared by backend, seed, and the step-18 wizard preview. Template-field labels live at `dashboard.documents.fields.*`.
- **Verification** — build clean; a rolldown-bundled node smoke harness (localStorage shim) ran the real backend end-to-end: 36/36 acceptance checks (policy codes, full happy path with exactly one audit entry per transition + expected notifications, append-only audit, reseed counts, persona queues). Bell deep-links to `/documents/:uuid` still fall to home until step 19 ships the detail route.

**Step 18 — M2 Flow 5 UI part A: documents registry + creation wizard (2026-06-12):** the two entry surfaces of BPMN 3.4, pure UI over the step-17 backend. No seed changes (`SEED_VERSION` stays `'6'`), no backend changes.

- **Registry at `/documents`** (replaces the step-16 placeholder; under `Protected`) — 4 underline tabs via `TabLabel` (Mening hujjatlarim = `creatorUuid` feed · Menga kelgan = `recipientUuid` non-DRAFT · Kelishuvda = client-side compose of `listMyApprovals` decision items + the creator's own IN_REVIEW docs · Arxiv = `archivedOnly` **grouped by `archivedAt` day** with date subheaders — the §2.2 daily-archive surface, hint copy "Imzolangan hujjatlar har kuni kun yakunida arxivlanadi"). All feeds key on `useActingEmployee()` so POV switches refetch every tab. `SearchInput` (number + title) + status `Select` inline on `md+` / bottom-sheet draft-Apply-Reset below (step-09 pattern, both halves in `registry/DocumentFilters.tsx`); `DocumentsTable` on `md+` (Raqam · Sarlavha · Turi · Yaratuvchi · Holat · Sana) / `DocumentsCardsMobile` card stack (min-h-16 tap targets) below; `Pagination` 20/page; full loading/empty/error states (archive has its own empty copy). Row click navigates to `/documents/:uuid` — **falls through the `*` catch-all to home until step 19 ships the detail route** (per the step prompt).
- **Wizard at `/documents/new`** (under `ProtectedNoShell`, employee-wizard chrome clone: mobile X-topbar + desktop header, pill stepper, `<form id="doc-wizard-step-N">` + `<Button form>` external submit, sticky `pb-safe` footer, dirty-confirm on close) — Step 1 Turi: two source cards (Shablon asosida / Tayyor faylni yuklash) + template gallery (name + description + field-count badge, ring-highlight select; switching templates clears stale `values`) or PDF/DOC/DOCX ≤ 10 MB pick-time-validated file chip (`formatBytes`, metadata-only). Step 2 Mazmun: Sarlavha + Kimga (employee `Combobox`) + Kim imzolaydi (optional, `__none__` sentinel = "ERI imzo talab qilinmaydi" → acceptance branch) + Maxfiylik Select, then per-`TemplateField` inputs (text/textarea/date/employee→Combobox) and a **live A4 preview** card running `renderTemplate` per keystroke with employee-kind uuid→FIO resolution mirroring the backend's `resolveTemplateValues`; the **dynamic zod schema** is built from `TemplateField[]` in `document.schema.ts` (`buildStep2Schema`; value shape typed `Record<string, z.ZodString>` — `z.ZodType<string>` leaves the input side `unknown` and breaks the RHF resolver match) and the form mounts only after the template resolves so the resolver never swaps mid-flight. Step 3 Kelishuv varaqasi: Switch (default on) → ordered participant builder (Combobox add excluding creator + dupes, numbered rows with FIO + position, up/down reorder buttons + remove — no DnD per master §17, min-1 validation) + sequential-only info banner; off → explanation line, step skippable. Step 4 Ko'rib chiqish: 3 summary cards (content / routing / participants) with Edit→`setCurrent` jumps; sticky-footer dual submit — **"Qoralama sifatida saqlash"** (`createDocument` → `/documents`) and **"Kelishuvga yuborish"** (`createDocument` + `submitDocumentForReview` → `/documents/:uuid`), with a `createdUuid` guard so a network flake between the two calls retries the submit without minting a second document. `DocumentValidationError` codes map to `dashboard.documents.errors.*` toasts; `MockNetworkError` keeps the store intact (retryable). Creation runs as the **acting persona** (`acting.employee.uuid` as `actorUuid`).
- **Shared chrome extraction** — `WizardStepper` moved to [`src/components/common/WizardStepper.tsx`](../dashboard/src/components/common/WizardStepper.tsx) (takes `steps`/`current`/`ariaLabelKey`) now that the document wizard is its second consumer; `features/employees/wizard/WizardStepper.tsx` became a thin binding that passes the employee-wizard steps. `StatusBadge` gained `IN_REVIEW` (cinnamon-soft/Hourglass) · `SIGNED` (emerald-soft/PenLine) · `CLOSED` (muted/CheckCheck) kinds + `common:status.*` keys. Home `QuickActions` gained the "Hujjat yaratish" tile (`/documents/new`, grid now 2/3/5-col).
- **Verification** — build clean (2923 modules); 12-route dev sweep all 200; scripted i18n audit: 109/109 referenced keys resolve, no Cyrillic/toast/JSX literals; a rolldown-bundled node harness over the real backend validated the tab feeds + both wizard submit paths end-to-end: **19/19 checks** (arxiv = 4 seeded docs all carrying `archivedAt`; XODIM template create → FIO-resolved `renderedBody`, no `«—»` leak; draft in Mening hujjatlarim; submit → IN_REVIEW; order-1 participant queued, order-2 not; creator's own IN_REVIEW feed; recipient inbox; kelishuv-off → APPROVED + acceptance queue item; number/title search). Two new `react-hooks/set-state-in-effect` lint errors are deliberate clones of the tolerated step-09/13 idiom (list-reset-before-fetch, sheet-draft-reset — 4 pre-existing instances).

**Step 19 — M2 Flow 5 UI part B: document detail + kelishuv actions + ERI signing + approvals queue (2026-06-12):** closes the BPMN 3.4 loop — every remaining transition now happens on `/documents/:uuid`, and `/approvals` replaces its step-16 placeholder. Registry rows and bell deep-links land on the real detail page.

- **Detail page at `/documents/:uuid`** (under `Protected`, [`features/documents/detail/`](../dashboard/src/features/documents/detail/)) — hero band (number + title + StatusBadge + MAXFIY cinnamon badge + creator→recipient + dates), persona-aware `DocumentActions` bar, then `lg:grid-cols-3`: left 2/3 `A4Preview`, right stack `ApprovalSheetCard` · `SignatureHistoryCard` · "Kimlar ko'rgan" · metadata card. `recordDocumentView` fires on mount (fire-and-forget, view-once; acting persona merged locally so the viewed list is immediately truthful); the whole page re-resolves on POV switch (`actingUuid` in the fetch deps). `A4Preview`: TEMPLATE → `aspect-[210/297]` white sheet with letterhead (number + date), serif title, `whitespace-pre-wrap` body, emerald stamp block when signed ("ERI bilan imzolangan — FIO — date — serial"), "Chop etish / PDF saqlash" button; UPLOAD → metadata-only file card ("Fayl namoyishi demo rejimida mavjud emas"). `window.print()` isolates the sheet via the new `.print-area` `@media print` rules in [`index.css`](../dashboard/src/index.css) (visibility-based, chrome hidden). `ApprovalSheetCard`: AssignmentTimeline rail, one row per current-round step (order + avatar + FIO + position + decision badge + italic comment + decidedAt), ring-emerald dot "Navbat shu yerda" on the actionable step, round `Select` when `round > 1` (halted rounds stay visible per BP-4). `SignatureHistoryCard`: signer + serial (links `/certificates`) + algorithm + signedAt + per-row 600 ms fake "Tekshirish" → "Imzo haqiqiy" badge.
- **Actions matrix** (`DocumentActions` — UI convenience only, the step-17 policy layer re-validates): DRAFT/REJECTED creator → Kelishuvga yuborish + Tahrirlash (+ O'chirish with AlertDialog, DRAFT only); IN_REVIEW current-order participant → Tasdiqlash / Izoh bilan tasdiqlash / Rad etish (all → `DecideDialog`, preselected; comment required ≥ 5 chars only for REJECTED, reject-consequence banner); everyone else in IN_REVIEW sees "Hozir {FIO} navbati"; APPROVED signer → `SignDialog` (ACTIVE-cert radio list, 6-digit PIN "serverga uzatilmaydi", 1.5 s `FakeEriSigner` ShieldCheck theatre → real `signDocument` → success state with serial); APPROVED no-signer recipient → Qabul qilish (confirm → CLOSED); SIGNED/CLOSED creator/recipient → Emailga yuborish (`EmailDialog`, "Yuborildi (demo)") + Chop etish (TEMPLATE only). [`FakeEriSigner.ts`](../dashboard/src/features/documents/FakeEriSigner.ts) is deliberately generic (`sign({resourceUuid})`) for step-21 letter reuse.
- **`/approvals` + sidebar badge** — `ApprovalsQueuePage` renders `listMyApprovals` in three labelled groups (Qaroringiz / Imzolash / Qabul qilish kutilmoqda, only non-empty), rows = number + title + creator + `formatRelative` waiting-since + chevron → detail; empty state "Sizni kutayotgan hujjatlar yo'q ✨". New [`useQueueStore`](../dashboard/src/stores/useQueueStore.ts) (`count` + `version`/`bump`): Sidebar fetches the count per acting persona (re-fetch on mount / POV switch / `bump()` after any decide-sign-accept-submit-delete via the detail page's `onChanged`), renders a cinnamon `9+`-capped pill on Kelishuvlar; the queue page pushes its own fresh count into the store to skip a second fetch.
- **Wizard edit mode** (`/documents/new?edit=<uuid>`) — the BP-4 rework loop: store gained `editing` + `hydrate()` (starts at step 2, dirty-baseline snapshot); the page fetches + policy-mirrors `not-editable`/`not-creator` before hydrating, then submits via `updateDraftDocument` (+ optional `submitDocumentForReview`, which bumps the round and reuses the rebuilt chain). Step 1 locks source + template picks (backend can't change them; UPLOAD file stays replaceable), Step 3 locks the kelishuv Switch (participants stay editable — the backend rebuilds the upcoming round's PENDING steps). **Backend deltas enabling this:** `DocumentEntity.values` now persists raw placeholder values (create/update/seed — model-change-first instead of faking in UI), `getDocument` returns steps of **all** rounds ordered (round selector needs halted history), `SEED_VERSION` bumped to `'7'`.
- **Verification** — build clean (2934 modules, 297.10 KB gzip); 14-route dev sweep all 200 (incl. `/documents/:uuid` + `/approvals`); scripted i18n audit: 162 static + 21 dynamic-family keys all resolve, no Cyrillic/toast/JSX literals; rolldown-bundled node harness over the real backend walked the full chain: **41/41 checks** (create with FIO-resolved values → submit → out-of-order guard → approve → comment-required guard → reject with frozen round-1 history → edit (non-creator blocked; values + upcoming-round chain rebuilt) → resubmit round 2 → approve-with-comment → APPROVED → not-signer/cert-invalid guards → sign → SIGNED + archived + SignatureRecord + notifications → Arxiv feed → not-deletable → email export → view-once ×2 personas → exactly-one-audit-entry-per-transition → draft delete → acceptance branch → audit append-only). Five new `react-hooks/set-state-in-effect` errors are deliberate clones of the tolerated step-09/13/18 idioms (3 dialog open-resets, 2 list-resets); `ApprovalSheetCard`'s round-follow uses the lint-clean adjust-during-render pattern instead.

**Step 20 — M2 Flow 6 part A: letters domain backend + registry (2026-06-13):** the BPMN 3.3 correspondence domain in the mock backend plus the registry UI at `/letters` (replaces the last step-16 `ComingSoonPage`, which was deleted from `router.tsx` along with its `coming-soon` i18n keys). The detail page + per-role actions are step 21 — `/letters/:uuid` stays unrouted (falls through the `*` catch-all to home, documents-step-18 precedent).

- **Types/backend** — `LetterDirection` · `LetterStatus` (10 states per master §15 / extended BP-3) · `LetterChannel` · `Letter` in `domain.ts` + zod mirrors; `AuditAction` += 8 `LETTER_*` actions (icons in all three audit icon maps + uz verbs; the audit page's resourceType filter finally gained `letter`). New `letters` localStorage table. `LetterValidationError` with 8 codes (`wrong-status`/`not-devonxona`/`not-rahbar`/`not-unit-head`/`not-executor`/`comment-required`/`missing-response`/`cert-invalid`) enforced inside every mutation against the acting employee uuid — persona resolution helpers documented in code: Devonxona = user carries `ROLE_DEVONXONA`; Rahbar = heads a root-level unit; unit head = heads the routed unit **or an ancestor**; executor membership checks the routed unit's **subtree** via `path` (a bo'lim head assigns from his sho'bas — the XODIM persona sits in the API Sho'ba under the Backend Bo'lim). Reads: `listLetters({direction,status,search,overdueOnly})` (search over number + subject + externalOrg) + `getLetter` (composed `LetterDetail`: signatures, linked incoming/outgoing, resolved names) + exported `isLetterOverdue` (deadline < today AND status not in the CLOSED/CLOSED_NO_RESPONSE/DISPATCHED terminal set). Mutations: `registerIncomingLetter` (Devonxona, auto-number `K-2026/NNNN`, audit-only — notifications start at routing) · `routeLetter` (Rahbar, → ROUTED, notify unit head) · `assignLetterExecutor` (unit head, → ASSIGNED, notify executor) · `startLetterExecution` (executor, → IN_PROGRESS; audited as `LETTER_EXECUTED` with `context.phase='started'` — CLAUDE.md wants every state change in the trail and the canonical action set has no separate verb) · `submitLetterExecution` (executor, → EXECUTED; comment path 7.1 vs response path 7.2 discriminated by `executionComment !== undefined`) · `acceptLetterExecution` (unit head; comment-only → CLOSED_NO_RESPONSE regardless of the signature flag — nothing to sign; response + requiresSignature → ON_SIGNATURE notifying the **root ancestor's** head; else → RESPONDED notifying Devonxona; executor always gets `LETTER_ACCEPTED`) · `signLetter` (Rahbar, ACTIVE-own-cert, `SignatureRecord` with `resourceType:'letter'`, → RESPONDED) · `dispatchLetter` (Devonxona, incoming → CLOSED + **creates** the OUTGOING reply: `CH-2026/NNNN`, terminal DISPATCHED, `linkedIncomingUuid`, addressee = incoming sender, inherits the response `fileMeta`; `LETTER_DISPATCHED` audited on both rows + `LETTER_CLOSED` on the incoming).
- **Registry UI** — `features/letters/`: `LettersPage` (Keluvchi/Chiquvchi `TabLabel` underline tabs with count pills fed by one direction-less fetch split client-side; CTA "Xat ro'yxatga olish" rendered only for the Devonxona persona — policy enforces regardless), `LetterFilters` (inline `SearchInput` + status `Select` + "Muddati o'tgan" `aria-pressed` toggle chip on `md+`; search + draft-state bottom sheet below — step-09 pattern), `LettersTable` (Raqam · Tashkilot · Mavzu · Bo'linma/Ijrochi stacked-resolved-names · Muddat with destructive text **+ `AlertTriangle` icon + sr-only label** when overdue — never colour alone · Holat) / `LetterCardMobile` (min-h-16, 2-line subject clamp), `RegisterLetterDialog` (`ResponsiveDialog` + RHF + zod `letter.schema.ts`: org/subject/channel Select/receivedAt≤today/deadline≥today with past-date refine/requiresSignature Checkbox/optional scan PDF-JPG-PNG ≤ 10 MB pick-time-validated metadata-only; fresh defaults per open so `receivedAt` is today's today; success toast carries the assigned number). `StatusBadge` gained the 9 letter kinds (already a config record — no refactor needed; `CLOSED` shared with documents).
- **Seed** — `SEED_VERSION` `'7'` → `'8'` (the step prompt said `'7'` but step 19 had consumed it — prompt anticipated this). 12 letters (deviation from the 10-letter table documented in `buildLetters`): see the seed-contents paragraph above for the anchor reconciliation (`…f001` RESPONDED, `…f002` ON_SIGNATURE, `…f003` = outgoing CH-2026/0001 carrying the step-17 signature).
- **Verification** — build clean (2941 modules, 304.17 KB gzip); 16-route dev sweep all 200 (incl. `/letters`); scripted i18n audit: 90 static + 4 dynamic-family keys resolve, no Cyrillic/toast/JSX literals; rolldown-bundled node harness over the real backend: **53/53 checks** (seed shape + anchors + signature resolution + linked-pair detail + overdue exactly-one + terminal-not-overdue + search by org/number/subject + full transition matrix with wrong-persona AND wrong-status codes per hop + K/CH counters continuing past the seed + dispatch dual-audit-trail + outgoing inherits response fileMeta + comment-only → CLOSED_NO_RESPONSE with NO outgoing row + notifications to the right persona at every hop + audit append-only). Two new `react-hooks/set-state-in-effect` lint errors are deliberate clones of the tolerated step-09/13/18/19 idioms (LetterFilters sheet-draft-reset, LettersPage list-reset-before-fetch). The 360 px observational check (cards, dialog-as-sheet, no horizontal scroll) joins the human-operator QA list.

**Step 21 — M2 Flow 6 part B: letter detail page + BP-3 timeline + per-role actions (2026-06-13):** closes the Flow-6 loop — every remaining transition happens on `/letters/:uuid`, and registry rows + bell deep-links to letters now resolve (they fell through the `*` catch-all to home since step 16).

- **Shared ERI extraction** — `FakeEriSigner` / `SignDialog` / `SignatureHistoryCard` moved `features/documents/` → **`features/_shared/eri/`** (git mv) so documents and letters share one copy. `SignDialog` parameterized: `documentUuid`→`resourceUuid` + optional `onSign?(certUuid)` (default `signDocument`; letters bind `signLetter` — **param orders differ**, hence a cert-only thunk) + `errorNamespace?: 'documents'|'letters'` + `successKey?`. The two document call sites updated to the new paths + `resourceUuid`; behaviour unchanged.
- **Detail feature** ([`features/letters/detail/`](../dashboard/src/features/letters/detail/)) — `LetterDetailPage` (hero with direction chip + overdue AlertTriangle+sr-only; `lg:grid-cols-3`: timeline + attachments left, metadata/linked-letter/signature-card right; re-resolves on POV switch via `actingUuid`; outgoing branch collapses the timeline + drops actions). `letterStations.ts` is a **pure** `(letter, audit)→ordered stations` mapper (past/current/future from status + audit; optional "Ijro boshlandi" shown only when it happened/is live; comment-only ends `closed-no-response`; signature station only when `requiresSignature && !commentOnly`; outgoing = registered→dispatched) — framework-free so the harness drives it directly; `LetterTimeline` renders it on the `ApprovalSheetCard` rail. `LetterActions` shows one primary action per BP-3 state via `LetterGate` booleans the page computes from acting persona vs letter (non-actors → "Hozir {lane} navbati" hint; terminal → muted line). Dialogs: `RouteDialog`, `AssignDialog` (Combobox **client-side filtered to the routed unit's subtree** — mirrors the backend `path.startsWith` membership; the backend throws a *plain* Error for a non-member, so the picker must only offer valid ones), `ExecuteDialog` (7.1 comment ≥10 / 7.2 response file-or-own-document), `DispatchDialog` (channel + read-only summary). `letterErrors.ts` maps `LetterValidationError`→toasts.
- **Seed** — `SEED_VERSION` `'8'`→`'9'`. New `buildLetterAudit(letters, employees, units, certificates)` emits the BP-3 audit trail for all 12 seeded letters (the timeline reads actor/date from audit; `buildAudit` never covered letters, so seeded mid-flight letters would render bare). Reproduces the live mutations' action + `context` shape exactly, timestamps linearly spaced across each letter's `createdAt`→`updatedAt`, merged into the audit table + re-sorted. Identity-changing → the version bump.
- **Verification** — build clean (2950 modules, 312.88 KB gzip), tsc clean, dev 200 on `/letters/:uuid`; scripted i18n audit 123/123 keys resolve (a missing `accept.*` block caught + fixed); rolldown node harness over the real backend + `buildLetterStations` **93/93** (12 seeded timelines + full live BP-3 walk + comment-only path + wrong-persona guards); an adversarial 4-dimension diff review with per-finding skeptic verification raised **1 confirmed low finding** — the `DispatchDialog` note field was bound but never sent (backend takes only `channel`) — **fixed** by removing the inert field. Six tolerated `set-state-in-effect` lint clones.

**Post-M2 HR-attachments + de-italic change (2026-06-13, out of band, before step 22):** three user-requested changes landed together. (1) The add-employee wizard step 3 gained a **required "Lavozim yo'riqnomasi"** (job-instruction) file attachment alongside the existing buyruqdan ko'chirma. (2) **Terminating an employee now requires** attaching the certified termination-order extract — the confirm `AlertDialog` became a `ResponsiveDialog` with a required `MetaFileField` (confirm gated until attached), enforced in `terminateEmployee` (new `EmployeeValidationError` codes `position-instruction-missing` / `termination-extract-missing`) and recorded in the audit context. (3) **All dashboard italics removed** — `italic` class dropped from the approval-comment + assignment-reason; the serif slogan/wordmark (login + sidebar) and A4 doc-preview title were italic because `index.html` loaded only the italic Fraunces variant, fixed by loading the roman variant (`Fraunces:opsz,wght@9..144,…`) so every `font-serif` spot is upright. (Follow-up: the two wizard step-3 attachments now sit side by side in a `md:grid-cols-2` block.) Shared refactor: [`MetaFileField`](../dashboard/src/components/common/MetaFileField.tsx) (metadata-only PDF/JPG/PNG ≤ 10 MB picker; constraints module-local per fast-refresh; generic `common:errors.file-*` + `common:actions.*-file` keys) extracted from the old `OrderExtractField`, now used by all three attachments; `OrderExtractField` deleted, orphan wizard i18n keys removed. `Employee` gained optional `positionInstruction` + `terminationOrderExtract` (both reuse the `EmploymentOrderExtract` shape, both locked out of `updateEmployee`); seed backfills `positionInstruction` onto every employee (`SEED_VERSION` `'9'` → `'10'`). Docs reconciled: `README.md` + `docs/product-specification.md` now state the position instruction is required **at creation** (was "after the profile exists") and document the termination-order extract. Verified: build clean (2950 modules, 313.41 KB gzip), tsc + lint clean, all 16 new/referenced i18n keys resolve, no Cyrillic/toast/italic leaks; a 5-lens adversarial diff review with per-finding skeptic verification raised 5 / **confirmed 2** (both **fixed**: terminate-dialog footer now uses the canonical `flex-col-reverse sm:flex-row` wrapper so mobile button order matches desktop; termination-only profile rows hidden until the employee is terminated) / rejected 3 (pre-existing or native-correct a11y patterns).

**Next:** step 22 (M2 home-surface integration + final M2 QA), then the observational QA sweep handed back to the human operator. After Lighthouse scores land in `QA_NOTES.md` and the six-viewport pass completes, v1 of the demo is "done" per the master prompt §17 quality bar. Beyond that the natural next-priorities are: writing `docs/user-manual-uz.md` (referenced in README but empty; now load-bearing since the dashboard is reachable to real users), filling `ru.json` for the v1.1 Russian-language pass, populating `docs/operations/` (deploy / backup / recovery / oncall runbooks), and the code-splitting + dead-shadcn-primitive cleanup items logged in `QA_NOTES.md` "Architectural / requires user prioritisation."

---

## Open questions / known gaps

- **Dashboard implementation in progress** — steps 01–14 are done; step 15 (`15-final-qa.md`, full responsive/i18n/accessibility pass) is the last one before declaring v1 of the demo done. The shadcn `form` primitive landed in step 10 — future forms can opt into the wrapper pattern when they benefit (nested arrays, dynamic field lists). The shadcn `slider` primitive landed in step 11 — reusable for any 0–100 range field. The typed `CertificateValidationError` + `CERTIFICATE_REJECTED` audit-action expansion landed in step 12. The typed `PasswordValidationError('current-wrong')` + `submitProfileChangeRequest` / `approveProfileRequest` / `changePassword` mock-backend mutations landed in step 13, plus `listAudit` gained `dateFrom` / `dateTo` filters. Step 14 wired the two-job GitHub Pages workflow (build dashboard → assemble combined `pages-dist/` → upload → deploy) and the spa-github-pages 404 fallback so deep-link refresh + direct paste work on the static host. Out-of-scope items deliberately deferred per master §17: real backend, real PFX parsing, real E-IMZO plugin, real SMS/email OTP, automated tests. The "Switch to employee POV" toggle deferred in step 13's prompt landed in step 16 as the POV switcher (5 personas via the user menu) — the profile request-approval flow itself still has no admin queue UI, but the multi-actor rail now exists for M2's approval/letter chains. The typed `DocumentValidationError` (11 policy codes), the full document mock-backend API, and the pure `renderTemplate` helper landed in step 17 — steps 18–19 consume them as pure UI work; letters (step 20) reuse `FileMeta`, `SignatureRecord`, and the `signatures` table as designed. Step 18 shipped the registry + creation wizard UI over that backend (shared `WizardStepper` extracted to `components/common`, `StatusBadge` document kinds, dynamic-zod `buildStep2Schema` pattern). Step 19 closed the Flow-5 loop: `/documents/:uuid` detail (A4 preview + print isolation, kelishuv varaqasi timeline with round history, signature history with fake verify, persona-aware actions incl. DecideDialog/SignDialog theatre), `/approvals` queue + sidebar badge (`useQueueStore`), wizard `?edit=` rework mode (backed by newly-persisted `DocumentEntity.values` + all-round `getDocument` steps, `SEED_VERSION` → `'7'`); document rows and bell deep-links now resolve. Step 20 shipped the Flow-6 letters backend (typed `LetterValidationError`, 8 mutations covering the full BP-3 transition matrix, `isLetterOverdue`) + the `/letters` registry with Devonxona's register dialog (`SEED_VERSION` → `'8'`, 12 seeded letters resolving the `…f001`–`…f003` placeholders). Step 21 shipped the `/letters/:uuid` detail page — BP-3 routing/execution timeline (driven by a pure `letterStations` mapper reading the audit trail) + persona-gated action bar (route/assign/start/submit/accept/sign/dispatch) reusing the now-shared `features/_shared/eri/` SignDialog, with `buildLetterAudit` seeding each letter's BP-3 trail (`SEED_VERSION` → `'9'`); **letter deep-links + registry rows now resolve** to the real detail page. The only M2 step left is **step 22** (home-surface integration of documents/letters/approvals + final M2 QA).
- **HR_ADMIN demo identity has been renamed three times in one day** (`Allaberganov Sardor Otabekovich` → `Umarov Jahongir Sobirovich` → `Pulatov Asilbek Karimovich`). The seed-versioning fix means future renames are a one-line `SEED_VERSION` bump in [`seed.ts`](../dashboard/src/lib/mock-backend/seed.ts) + the four prompt-doc spots — both documented in [`LESSONS.md`](./LESSONS.md). The auth store's `refreshSessionUser` handles silently updating already-cached sessions so users never see stale names. Existing browsers with the old `'1'` or `'2'` flag will reseed on next load.
- **User manual (Uzbek)** — `docs/user-manual-uz.md` is referenced in the README but doesn't exist yet. Now load-bearing since the dashboard is live and reachable to real users at `sardorallaberganov.github.io/devon-landing/dashboard/` — write before opening the demo to non-technical stakeholders.
- ~~**Build lessons file**~~ — was empty; [`ai_context/LESSONS.md`](./LESSONS.md) now exists and carries the full-width-main decision (2026-05-25). Append future cross-step decisions as they surface.
- **Operations runbook** — `docs/operations/` is referenced but the folder is empty. Needs `deployment.md`, `backup.md`, `recovery.md`, `oncall.md` populated for sysadmins.
- ~~**BPMN diagrams**~~ — resolved 2026-06-12: the 4 swim-lane PNGs were extracted from `docs/Plyma 19.03.2026.docx` into [`docs/bpmn/`](../docs/bpmn/) with an index README mapping each chart to its milestone.
- **ADRs** — `docs/adr/` is empty. The first ADRs should retroactively capture: choice of 4-level org hierarchy as a first-class model, signed-document immutability triple-layer enforcement, on-premise deployment as a hard constraint.
- **Real client logos** — the landing page's trust band has gray placeholders + a `<!-- TODO: Replace with real client logos -->` comment.
- **TLH internal inconsistency (flag for the doc owner)** — `docs/Plyma 19.03.2026.docx` lists only 7 module sections (§2.1–2.7): **Incoming/Outgoing Letters is missing as a numbered module** even though its BPMN (§3.3) is present and the README/product-spec canon counts 8 modules. Per the 2026-06-12 planning decision, the 8-module canon stands and letters stay in scope (dashboard M2 builds them). Also: the TLH's RBAC line mentions a "Mehmon" (guest) role — consistent with the glossary's "planned post-v1.0" entry, not a v1.0 scope change.

---

## Naming history

| Name | Era | Status |
|---|---|---|
| **PLYMO** | Early concept, pre-2025 | Legacy. Appears in the earliest spec. |
| **PLYMA** | 2025 spec phase | Legacy. Appears in `docs/Plyma_Technical_Spec_v1.0.docx`, early landing-page drafts. From Greek *plimo* — "flow." |
| **Devon** | 2026+ (current) | Shipping name. All new artifacts use Devon. |

When you encounter PLYMO or PLYMA in legacy material, mentally substitute Devon. Same product.

---

## How to think about this file

Update this snapshot when any of the following change:

- Module status flips (a module moves from "in spec" to "in production" or gets de-scoped)
- Roadmap reshuffles
- A canonical doc is added or removed
- The competitive picture shifts (new competitor, EDoc ships something significant)
- Brand voice or language defaults change
- The landing page's overall structure changes (not every visual tweak — those go in `HISTORY.md`)
- A new "open question" emerges or an existing one gets resolved

If a change is too granular for this file, it belongs in `HISTORY.md`. If it's structural, both files get updated.
